# Tasks: Architect Kernel and Exclusive Architecture Playbooks

## Status legend

- pending: not started
- in_progress: actively being implemented
- done: completed and verified for the stated task
- blocked: cannot proceed without input or prerequisite
- cancelled: intentionally no longer needed

## Task table

| ID | Status | Owner | Files / areas | Technical objective | Covers | Dependencies | Verification | Notes |
|---|---|---|---|---|---|---|---|---|
| T-001 | done | Developer | `templates/skills/local-architecture/SKILL.md`, `src/lib/paths.ts`, `src/lib/paths.test.ts`, `src/lib/skills.test.ts` | Add `local-architecture` as a valid core managed skill and move the complete local handoff schema/protocol into it without weakening existing local contracts, tasks, checkpoints, persistence, or exit gates. | AC-03, AC-06, AC-08 | none | Run `npm test -- src/lib/paths.test.ts src/lib/skills.test.ts`; inspect the new skill frontmatter/marker and confirm local schema invariants. | Prefer parameterized assertions for both core skills. Do not change other agent prompts. |
| T-002 | done | Developer | `templates/architect.md`, `src/lib/templates.test.ts` | Refactor Architect into the common kernel: classify mode by boundary impact, declare Mode/Playbook/Reason, load one architecture playbook, allow optional domain skills, handle safe transitions, and remove embedded mode-specific schemas after transferring them. | AC-01, AC-02, AC-07, AC-09, AC-10 | T-001 | Run `npm test -- src/lib/templates.test.ts`; inspect that unrelated permission and Reviewer-invocation invariants still pass. | Do not broaden Architect edit permissions. Kernel may retain only genuinely shared handoff transport/safety rules. |
| T-003 | done | Developer | `templates/skills/cross-repo-architecture/SKILL.md`, `src/lib/skills.test.ts`, `src/lib/templates.test.ts` | Rewrite the cross playbook around responsibilities, shared contracts, compatibility, development-order constraints, and repo drafts; define `brief.md` + `repos/{repo}.md` only and remove local planning and post-commit operational ownership. | AC-04, AC-05, AC-06, AC-07, AC-09, AC-12 | T-002 | Run `npm test -- src/lib/skills.test.ts src/lib/templates.test.ts`; verify cross prompt invariants and the four classification scenarios. | Do not add cross tasks/checkpoints or a Cross Auditor. Preserve escalation for impossible shared contracts. |
| T-004 | done | Developer | `src/commands/init.test.ts`, `src/commands/skills.test.ts`, `src/commands/uninstall.test.ts`, `scripts/validate-dist-skill-lookup.mjs`, bounded discovery under `src/**/*.test.ts` for hard-coded `CORE_SKILLS`/`cross-repo-architecture` fixtures | Update managed lifecycle and packaging expectations from one core architecture skill to both core architecture skills while preserving generic production iteration and conflict safety. | AC-08, AC-10 | T-001, T-003 | Run `npm test -- src/commands/init.test.ts src/commands/skills.test.ts src/commands/uninstall.test.ts` and `npm run validate-dist`. | Do not special-case `local-architecture` in production code where `CORE_SKILLS` iteration already handles it. Record any additional test file in `progress.md` before editing. |
| T-005 | done | Developer | `templates/architect.md`, `templates/skills/local-architecture/SKILL.md`, `templates/skills/cross-repo-architecture/SKILL.md`, `src/lib/paths.ts`, `src/lib/paths.test.ts`, `src/lib/skills.test.ts`, `src/lib/templates.test.ts`, `src/commands/init.test.ts`, `src/commands/skills.test.ts`, `src/commands/uninstall.test.ts`, `scripts/validate-dist-skill-lookup.mjs` | Perform final protocol consistency review and repository validation, ensuring no stale universal three-file handoff rule or cross operational ownership remains and no out-of-scope agent changed. | AC-01, AC-02, AC-03, AC-04, AC-05, AC-06, AC-07, AC-08, AC-09, AC-10, AC-11, AC-12 | T-001, T-002, T-003, T-004 | Run `npm test`, `npm run typecheck`, `npm run build`, `npm run validate-dist`, and `npm run smoke`; inspect `git diff -- templates/architect.md templates/skills src/lib/paths.ts src/lib/paths.test.ts src/lib/skills.test.ts src/lib/templates.test.ts src/commands/init.test.ts src/commands/skills.test.ts src/commands/uninstall.test.ts scripts/validate-dist-skill-lookup.mjs`. | Confirm only Architect, architecture skills, catalog wiring, tests, and packaging validation changed. Restart OpenCode is required after installed definitions are updated. |
| T-006 | done | Developer | `src/commands/init.ts`, `src/commands/init.test.ts`, `src/lib/agents.ts`, `src/lib/agents.test.ts`, `src/lib/skills.ts`, `src/lib/skills.test.ts`, `src/lib/frontmatter.ts`, `src/lib/frontmatter.test.ts`, `README.md` | Integrate automatic reconciliation of the architecture bundle into normal `opencode-path init`: always retain/reconcile active Architect, create missing Architect only through existing selection, reconcile both core skills, preserve/reapply valid Architect `model`, classify create/update/unchanged/conflict states, and integrate aggregate preview/approval/`--yes`/dry-run/apply/results/idempotence/restart behavior. | AC-08, AC-10, AC-14, AC-15, AC-16 | T-005 | Run `npm test -- src/lib/agents.test.ts src/lib/skills.test.ts src/lib/frontmatter.test.ts src/commands/init.test.ts`; separately prove interactive approval, interactive rejection, `--dry-run`, `--dry-run --yes`, and `--yes`; active retention, missing selected/unselected behavior, absence of removal; model-preserving update/rerun; malformed model conflict; mixed states; scope isolation; and result messages. | Add no command, flag, second prompt, or init-removal state. `--yes` authorizes displayed marked replacements; dry-run always prevents writes. Preserve only valid non-empty Architect `model`; replace other authorized marked drift; never overwrite/delete conflicts. |
| T-007 | done | Developer | `templates/architect.md`, `templates/skills/local-architecture/SKILL.md`, `templates/skills/cross-repo-architecture/SKILL.md`, `src/lib/templates.test.ts` | Restore the complete Mode 3 worktree preflight in the common kernel, retain mode-specific artifact schemas in their playbooks, and prove sibling-directory/worktree/branch collision stops plus the four-item confirmation and explicit approval. | AC-05, AC-10, AC-13 | T-005 | Run `npm test -- src/lib/templates.test.ts`; verify assertions for sibling directory, `git worktree list`, `feature/{slug}`, no reuse/overwrite/auto-increment, worktree/work-folder/branch/base display, and approval before creation. | Local remains three-file; cross remains `brief.md` + `repos/{repo}.md` only. Playbooks reference but do not weaken the kernel preflight. |
| T-008 | done | Developer | `src/lib/templates.test.ts`, `src/lib/skills.test.ts`, `src/lib/frontmatter.test.ts`, `src/commands/init.test.ts` | Strengthen protocol traceability tests for local task/checkpoint/progress schemas, AC mapping, binding cross-draft consumption, local/cross artifact exclusions, exact mode scenarios, unavailable-playbook stop, active-Architect retention/no-removal semantics, model-preserving canonicalization, interactive/`--yes`/dry-run reconciliation safety, and worktree confirmation. | AC-02, AC-03, AC-05, AC-06, AC-07, AC-08, AC-09, AC-13, AC-14, AC-15, AC-16 | T-006, T-007 | Run `npm test -- src/lib/templates.test.ts src/lib/skills.test.ts src/lib/frontmatter.test.ts src/commands/init.test.ts`; confirm each listed behavior has a focused assertion that would fail if the corresponding contract rule were removed. | Do not substitute broad substring presence for the specific stop/schema/model/authorization/confirmation behaviors identified by Auditor. |
| T-009 | done | Developer | `templates/architect.md`, `templates/skills/local-architecture/SKILL.md`, `templates/skills/cross-repo-architecture/SKILL.md`, `src/commands/init.ts`, `src/commands/init.test.ts`, `src/lib/agents.ts`, `src/lib/agents.test.ts`, `src/lib/skills.ts`, `src/lib/skills.test.ts`, `src/lib/frontmatter.ts`, `src/lib/frontmatter.test.ts`, `src/lib/templates.test.ts`, `README.md`, `scripts/validate-dist-skill-lookup.mjs` | Perform post-audit final consistency and regression validation, confirm only the approved scope changed, and collect evidence for a new final Reviewer checkpoint without altering historical verdicts/findings. | AC-01, AC-02, AC-03, AC-04, AC-05, AC-06, AC-07, AC-08, AC-09, AC-10, AC-11, AC-12, AC-13, AC-14, AC-15, AC-16 | T-006, T-007, T-008 | Run `npm test`, `npm run typecheck`, `npm run build`, `npm run validate-dist`, and `npm run smoke`; inspect the scoped diff and run the repository's whitespace/diff check. | Do not mark Auditor findings resolved; record implementation evidence and request Reviewer, then leave final Auditor re-audit to the user. |

## Checkpoints

| ID | Included tasks | Intended ACs closed | Reviewer focus | Expected evidence | Reviewer required |
|---|---|---|---|---|---|
| CP-01 | T-001, T-002 | AC-01, AC-02, AC-03, AC-06, AC-07, AC-09, AC-10 | Verify the kernel/playbook boundary, local behavior preservation, explicit mode classifier, playbook exclusivity, and unchanged Architect permissions. | Diff limited to Architect kernel, new local skill, core catalog, and focused tests; focused Vitest results recorded in `progress.md`. | yes |
| CP-02 | T-003, T-004 | AC-04, AC-05, AC-08, AC-09, AC-10, AC-12 | Verify the minimal cross artifact shape, compatibility-only ownership, absence of local/post-commit workflow, and complete two-core-skill lifecycle coverage. | Cross skill and lifecycle-test diff; focused test results and `npm run validate-dist` output recorded in `progress.md`. | yes |
| CP-03 | T-005 | AC-01, AC-02, AC-03, AC-04, AC-05, AC-06, AC-07, AC-08, AC-09, AC-10, AC-11, AC-12 | Final feature review of the full diff, all protocol invariants, out-of-scope protection, and complete validation evidence. | Complete scoped diff, all five validation commands passing, prior checkpoint verdicts resolved, and final evidence in `progress.md`. | yes |
| CP-04 | T-006 | AC-08, AC-10, AC-14, AC-15, AC-16 | Verify normal-init architecture reconciliation end to end: add/restore-only active retention, missing Architect selection, both always-desired core skills, exact allowlist, valid-model preservation/malformed conflict, marker safety, aggregate preview, interactive approve/reject, `--yes`, dry-run precedence, mixed outcomes, idempotence, messages, restart semantics, and no other-agent/optional/Graphify mutation. | Reconciliation/frontmatter implementation diff, focused agent/skill/frontmatter/init tests, README behavior documentation, and recorded model-preserving before/after/idempotent evidence for all four execution modes. | yes |
| CP-05 | T-007, T-008 | AC-02, AC-03, AC-05, AC-06, AC-07, AC-08, AC-09, AC-10, AC-13, AC-14, AC-15, AC-16 | Verify restored Mode 3 safety, unchanged local/cross schemas, stop-on-missing-playbook, exact init retention/model/authorization regression assertions, and absence of weakened prose-only coverage. | Kernel/playbook/test diff plus focused template/skill/frontmatter/init test results and explicit mapping from each Auditor finding to a regression assertion. | yes |
| CP-06 | T-009 | AC-01, AC-02, AC-03, AC-04, AC-05, AC-06, AC-07, AC-08, AC-09, AC-10, AC-11, AC-12, AC-13, AC-14, AC-15, AC-16 | New final feature review after Auditor FAIL; validate the complete accumulated diff, all ACs, reconciliation/model/authorization safety, worktree safety, scope control, and full evidence. | Complete scoped diff, CP-04/CP-05 PASS evidence, all repository validation commands passing, and historical Reviewer/Auditor entries preserved. | yes |

## Coverage notes

- AC-01 is covered by T-002 and T-005.
- AC-02 is covered by T-002, T-005, T-008, and T-009.
- AC-03 is covered by T-001, T-005, T-008, and T-009.
- AC-04 is covered by T-003 and T-005.
- AC-05 is covered by T-003, T-005, T-007, T-008, and T-009.
- AC-06 is covered by T-001, T-003, T-005, T-008, and T-009.
- AC-07 is covered by T-002, T-003, T-005, T-008, and T-009.
- AC-08 was partially covered by T-001/T-004/T-005 under the superseded install-only wording and is now covered by T-006, T-008, and T-009.
- AC-09 is covered by T-002, T-003, T-008, and T-009.
- AC-10 is covered by T-002, T-004, T-005, T-006, T-007, and T-009.
- AC-11 is covered by T-005 and T-009.
- AC-12 is covered by T-003, T-005, T-007, and T-009.
- AC-13 is covered by T-007, T-008, and T-009.
- AC-14 is covered by T-006, T-008, and T-009.
- AC-15 is covered by T-006, T-008, and T-009.
- AC-16 is covered by T-006, T-008, and T-009.
- Every implementation task belongs to at least one checkpoint.
- CP-01 through CP-03 and T-001 through T-005 are retained as historical completed work. The Auditor FAIL reopens feature closure through T-006 through T-009 and CP-04 through CP-06; it does not rewrite the historical statuses or verdicts.

## Auditor notes

| Date | Related task | Severity | Status | Finding / resolution note | Suggested follow-up |
|---|---|---|---|---|---|
| 2026-07-16 | T-004, T-005 / AC-08 | major | open | Existing managed installations do not receive the rewritten Architect or cross playbook through re-init: skill state is marker-only, `init` plans changes only for missing core skills, and active skills are left unchanged (`src/lib/skills.ts:238-249`, `src/commands/init.ts:443-462`, `src/commands/init.ts:510-533`). Agent management likewise leaves an active Architect unchanged. This contradicts the claimed managed update/re-init path in `brief.md:21,215,242`. | Return to Architect to define the supported update path. Then either wire a safe managed refresh for active agent/core-skill templates with conflict tests, or narrow the contract and document an explicit removal/reinstall flow before closure. |
| 2026-07-16 | T-002, T-005 / local safety invariants | major | open | The refactor weakens the previously explicit dedicated-worktree safety protocol. `templates/architect.md:223-231` and `templates/skills/local-architecture/SKILL.md:74-90` say to verify target/branch/collisions, but no longer require the concrete sibling-directory, registered-worktree, branch-existence checks or the four-item confirmation that existed in the prior Architect prompt. This is not the complete preserved local collision/worktree procedure required by `brief.md:45,96,103,147`. | Restore the explicit preflight and confirmation requirements in the kernel/local playbook as appropriate, and add regression assertions for directory, worktree, branch, base, and explicit approval checks. |
| 2026-07-16 | T-001, T-002, T-003 / AC-02, AC-03, AC-06, AC-09 | minor | open | Static regression coverage is weaker than the completion claims: `src/lib/templates.test.ts:232-270` checks the local Implementation Contract and exit gate but not the local task/progress schemas, coverage mapping, or cross-draft consumption; `src/lib/templates.test.ts:272-296` checks scenario prose as substrings and does not assert stop-on-unavailable-playbook. Current prompt content is present, but these regressions could remain misleadingly green. | Add focused assertions for the local task/checkpoint/progress schemas, cross-draft consumption, mode+scenario table rows, and stop-if-unavailable behavior. |
| 2026-07-16 | T-006 / AC-08, AC-14 | major | open | The revised contract requires Architect deselection/removal to win (`brief.md:149-151,281,307`), but normal `init` is explicitly add/restore-only and forcibly retains every active agent (`src/commands/init.ts:821-843`). There is no existing normal-init Architect removal state for Developer to preserve or test. | Architect must either remove the impossible deselection/removal requirement and define active Architect as retained during normal init, or explicitly authorize a broader init-removal UX change and its compatibility tests. Developer must not invent this behavior. |
| 2026-07-16 | T-006 / AC-08, AC-14, AC-15 | major | open | Whole-byte canonical replacement of managed `architect.md` can erase a supported `model:` value written by `opencode-path models` or init (`src/lib/frontmatter.ts:88-139`, `src/commands/init.ts:1005-1097`). The contract discusses replaceable marked customizations but does not decide whether tool-managed mutable frontmatter must survive architecture reconciliation. | Architect must define canonical comparison/update semantics for supported mutable agent state: preserve/reapply `model` (and name any other preserved fields), or explicitly accept and surface its loss with focused tests. Update T-006 files if a frontmatter helper is required. |
| 2026-07-16 | T-006 / AC-14, AC-15 | major | open | The contract requires visible warning and explicit approval before replacing marked drift, but does not define behavior for normal `init --yes`, which skips the final confirmation. It is unclear whether `--yes` authorizes destructive architecture replacement, skips such updates, or requires a special exception. | Architect must bind `--yes` semantics and add focused dry-run/interactive/decline/`--yes` tests before Developer implements reconciliation. |
| 2026-07-16 | T-006 / AC-08, AC-14, AC-15, AC-16 | major | contract resolved; implementation pending | Architect resolved the three focused pre-development findings in `brief.md:49-51,66-71,149-183,304-312`: normal init remains add/restore-only and retains active Architect; valid non-empty `model` is reapplied to canonical content while malformed marked frontmatter/model is preserved as conflict; `--yes` authorizes displayed replacements and `--dry-run` always prevents writes. T-006/T-008 and CP-04/CP-05 now require direct regression evidence. | Developer may begin T-006. Keep the historical findings open until implementation and Reviewer evidence prove the contract; do not treat this contract resolution as feature closure. |
| 2026-07-16 | T-004–T-008 / historical Auditor findings | major | resolved | T-006 implements bounded normal-init reconciliation with retention/conflict/authorization tests; T-007 restores the complete shared Mode 3 preflight; T-008 adds focused local/cross schema, unavailable-playbook, model, authorization, and worktree assertions. CP-04, CP-05, and CP-06 all have Reviewer PASS, and Auditor reproduced the 457-test suite plus build validations. | Historical findings are closed by implementation evidence, subject to the new final-audit findings below. |
| 2026-07-16 | T-006 / AC-16 | major | open | Exact model preservation is not guaranteed for every value accepted by `isValidAgentModel`. `src/lib/frontmatter.ts:20-21` accepts any non-empty string, but `setModelInContent` writes it as an unquoted YAML scalar at `src/lib/frontmatter.ts:179-183`. A previously quoted valid value such as `provider/model #pinned` is rewritten as `model: provider/model #pinned`, which YAML reads as `provider/model`; similar scalar-sensitive values can become invalid or change meaning. `planArchitectReconciliation` uses this path at `src/lib/agents.ts:224-238`. | Emit the model as a YAML-safe scalar while preserving the intended string value, and add reconciliation/idempotence tests for `#`, `:`, quotes, and leading/trailing whitespace or narrow the accepted model grammar explicitly. |
| 2026-07-16 | T-006 / AC-14, AC-16 | major | open | Apply-time safety only rechecks the managed marker. After preview, `applyArchitectReconciliation` writes the stale `expectedContent` at `src/lib/agents.ts:274-287` without re-parsing the current model/frontmatter. If the valid model changes or the marked file becomes malformed while confirmation is pending, the approved apply can erase the newer model or overwrite a file that the contract says must be preserved as conflict. No test mutates Architect between plan and apply. | Revalidate/replan the current Architect immediately before writing (or reject a changed snapshot as conflict) and add tests for valid-model and malformed-frontmatter changes between planning and apply. |
| 2026-07-16 | T-006 / decision 13 | minor | open | The binding brief requires no-change, declined, dry-run, and conflicts-only outcomes to state that architecture definitions were not changed and no restart is required (`brief.md:68`). The early returns at `src/commands/init.ts:1186-1230` print generic no-change/cancel/dry-run output but omit that explicit architecture/no-restart statement; tests assert only absence of `Restart opencode`. | Print the explicit no-architecture-change/no-restart message on each early-return outcome and assert it positively. |
| 2026-07-16 | T-006 / final focused remediation | major | resolved | `setModelInContent` now serializes YAML-sensitive models safely and tests exact parse/write/parse preservation; `applyArchitectReconciliation` replans immediately before write and tests model/frontmatter/marker/create races; all no-write exits emit and assert the explicit no-change/no-restart message. Reviewer returned focused PASS, and Auditor independently reproduced 162 focused tests, 474 full tests, typecheck, build, distribution validation, smoke, and diff checks. | No further code follow-up required for the audited scope. Retain manual interactive/OpenCode runtime validation as optional operational evidence. |
